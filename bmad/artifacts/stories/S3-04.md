# S3-04 — config.yaml support

**Sprint:** 3 | **Effort:** S | **Dépend de:** S2-05

## Acceptance Criteria

- [ ] `config.yaml` à la racine du projet charge les paramètres par défaut
- [ ] Clés: `imap_port`, `smtp_port`, `imap_host`, `smtp_host`, `local_password`, `tls`, `log_level`
- [ ] CLI args surchargent config.yaml
- [ ] Si `config.yaml` absent → comportement actuel (defaults CLI)
- [ ] `src/config.py` module `load_config()` retourne dict mergé config+args
