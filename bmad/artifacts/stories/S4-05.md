# S4-05 — Reconnexion indépendante par compte

**Sprint:** 4 | **Effort:** S | **Status:** planned | **Dépend de:** S4-02

## Acceptance Criteria

- [ ] `SessionManager.reconnect(label)` tente jusqu'à 3 fois avec backoff
- [ ] Échec reconnexion d'un compte → log ERROR, autres comptes non affectés
- [ ] `main.py` détecte session expirée (exception API 401) → appelle `reconnect(label)`
- [ ] Reconnexion transparente pour le client IMAP (pas de déconnexion)
