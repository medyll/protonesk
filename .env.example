# ⚠️ DEPRECATED — DO NOT USE FOR CREDENTIALS

# This file is kept for backwards compatibility ONLY.
# DO NOT store credentials here (AI can read .env files).

# ✅ USE INSTEAD:
# python src/secrets.py setup

# This stores credentials in OS keyring (AI cannot access).
# See SECURITY.md for details.

# === Non-sensitive config only ===

# Open WebUI integration (optional, not credentials)
OPEN_WEBUI_URL=http://localhost:11434

# Log level
LOG_LEVEL=INFO
